name: your_project_name
description: A new Flutter project.

publish_to: 'none' # Remove isso se quiser publicar no pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0  # Adicione esta linha

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
